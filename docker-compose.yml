services:
  app:
    build: .
    restart: unless-stopped
    ports:
      - "${APP_PORT:-4001}:80"
    environment:
      APP_NAME: "${APP_NAME:-Piramid}"
      APP_ENV: "${APP_ENV:-production}"
      APP_DEBUG: "${APP_DEBUG:-true}"
      APP_KEY: "${APP_KEY:-base64:j6g+0eK8Y6T6o7rQ4yvVf8Uv9Hq1c+O6ZpB5wzQy8l4=}"
      APP_URL: "${APP_URL:-http://localhost:4001}"
      LOG_CHANNEL: "${LOG_CHANNEL:-stack}"
      LOG_LEVEL: "${LOG_LEVEL:-error}"

      DB_CONNECTION: pgsql
      DB_HOST: "${DB_HOST:-pgpiramid}"
      DB_PORT: 5432
      DB_DATABASE: "${DB_DATABASE:-qurban_pyramid}"
      DB_USERNAME: "${DB_USERNAME:-qurban_app}"
      DB_PASSWORD: "${DB_PASSWORD:-qurban_dev_secret}"

      SESSION_DRIVER: "${SESSION_DRIVER:-database}"
      CACHE_STORE: "${CACHE_STORE:-database}"
      QUEUE_CONNECTION: "${QUEUE_CONNECTION:-database}"
      FILESYSTEM_DISK: "${FILESYSTEM_DISK:-local}"

      # Integrasi pihak ketiga (Midtrans Gateway)
      MIDTRANS_MERCHANT_ID: "${MIDTRANS_MERCHANT_ID:-G778086072}"
      MIDTRANS_SERVER_KEY: "${MIDTRANS_SERVER_KEY:-SB-Mid-server-UWdxhKL11SJqG3c8T0TFyfvo}"
      MIDTRANS_CLIENT_KEY: "${MIDTRANS_CLIENT_KEY:-SB-Mid-client-yq36YrCJTtq0Cb2A}"
      MIDTRANS_IS_PRODUCTION: "${MIDTRANS_IS_PRODUCTION:-false}"
      CLOUDINARY_CLOUD_NAME: "${CLOUDINARY_CLOUD_NAME:-}"
      CLOUDINARY_API_KEY: "${CLOUDINARY_API_KEY:-}"
      CLOUDINARY_API_SECRET: "${CLOUDINARY_API_SECRET:-}"
      CLOUDINARY_UPLOAD_FOLDER: "${CLOUDINARY_UPLOAD_FOLDER:-qurban-pyramid}"
      VAPID_PUBLIC_KEY: "${VAPID_PUBLIC_KEY:-BLZ49N4zM3bO5wW11wUqyX7E9qK4h11w3UqyX7E9qK4h11w3UqyX7E9qK4h11w3UqyX7E9qK4=}"
      VAPID_PRIVATE_KEY: "${VAPID_PRIVATE_KEY:-x_UqyX7E9qK4h11w3UqyX7E9qK4h11w3UqyX7E9qK4=}"
      VAPID_SUBJECT: "${VAPID_SUBJECT:-mailto:admin@piramidqurban.com}"
    volumes:
      - storage_data:/var/www/html/storage/app/public
    depends_on:
      pgpiramid:
        condition: service_healthy

  pgpiramid:
    image: postgres:17-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: "${DB_DATABASE:-qurban_pyramid}"
      POSTGRES_USER: "${DB_USERNAME:-qurban_app}"
      POSTGRES_PASSWORD: "${DB_PASSWORD:-qurban_dev_secret}"
    ports:
      - "${DB_PORT:-5434}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USERNAME:-qurban_app} -d ${DB_DATABASE:-qurban_pyramid}"]
      interval: 5s
      timeout: 5s
      retries: 5
      start_period: 10s

volumes:
  pgdata:
  storage_data:
